ext {
    minSdkVersion = 24
    compileSdkVersion = 36
    targetSdkVersion = 36
    androidxActivityVersion = '1.11.0'
    androidxAppCompatVersion = '1.7.1'
    androidxCoordinatorLayoutVersion = '1.3.0'
    androidxCoreVersion = '1.17.0'
    androidxFragmentVersion = '1.8.9'
    coreSplashScreenVersion = '1.2.0'
    androidxWebkitVersion = '1.14.0'
    junitVersion = '4.13.2'
    androidxJunitVersion = '1.3.0'
    androidxEspressoCoreVersion = '3.7.0'
    cordovaAndroidVersion = '14.0.1'
    // Required by @capgo/capacitor-printer's own build.gradle (androidx.print / androidx.documentfile) —
    // it falls back to these same values if left unset here, but declaring them explicitly at the
    // root keeps every module resolving the exact same version instead of each guessing its own default.
    androidxDocumentFileVersion = '1.0.1'
    androidxPrintVersion = '1.0.0'
}